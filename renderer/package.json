{
  "name": "x-tools",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @x-tools/app-main dev",
    "build": "pnpm --filter @x-tools/app-main build",
    "typecheck": "tsc --noEmit -p apps/main/tsconfig.json",
    "preview": "pnpm --filter @x-tools/app-main preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "catalog:"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}